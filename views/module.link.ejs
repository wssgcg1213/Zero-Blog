<div id="link">
	<div class="wrap">
		<em class="format-ico link-ico">友链</em>
		<div class="format-content">
			<div class="link-content">
                <% for(var i = 0; i<links.length; i++){ %>
				<a href="<%= links[i].url %>" rel="nofollow"><%= links[i].content %></a>
                <% } %>
			</div>
		</div>
	</div>
</div>